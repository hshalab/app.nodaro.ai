---
"@nodaro/shared": minor
---

Add `flux-fill` (BFL FLUX Fill Pro via Replicate) to the image-to-image provider catalog and to `I2I_MASK_SUPPORT` — a second mask-capable inpainting provider alongside `ideogram-edit`. White = edit area, matching the painter / generate-mask polarity.
