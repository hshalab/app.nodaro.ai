---
"@nodaro/sdk": minor
---

feat: `UserIdentity.isAdmin` on `client.me()` (descriptive only — server-side `requireAdmin` stays the enforcement), plus public marketplace resources: `client.templates.{browse,get,clone}` and `client.tutorials.list()`. Admin/creator template surfaces stay deliberately outside the public SDK.
