---
"@nodaro/cli": patch
---

Docs: correct the production `--base-url` example from `https://api.nodaro.ai` (a domain that does not exist) to `https://app.nodaro.ai`. The wrong example has misled users into configuring MCP connectors and CLI profiles against an unresolvable host.
