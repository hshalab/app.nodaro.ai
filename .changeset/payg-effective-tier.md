---
"@nodaro/shared": minor
---

New `effective-tier` module: `resolveStoredTier`, `resolveEffectiveTier` (a stored-free profile with net lifetime top-up credits derives the `payg` tier), `isPaygRetentionActive` + `PAYG_RETENTION_DAYS`. Pipeline tier maps gain `payg` keys.
