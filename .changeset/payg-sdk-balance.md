---
"@nodaro/sdk": minor
---

`UserBalance` gains `effectiveTier` — the derived entitlement tier ("payg" for pay-as-you-go users: no subscription, purchased credits, full model access). `tier` keeps the stored billing tier for back-compat.
