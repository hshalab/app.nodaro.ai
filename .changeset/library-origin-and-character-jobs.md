---
"@nodaro/sdk": minor
---

feat: `LibraryAsset.source` / `sourceDetail` + a `source` filter on `client.library.list()` — media can now be labeled and filtered by the surface that produced it (browser extension, studio, CLI, MCP, …) without hydrating each item's job.
